.row-fluid
  .span12
    %p= @resource.name + "님의 계정에 다수의 잘못된 로그인 시도가 발생하여 잠금 상태로 전환되었습니다."
    %p 아래 링크를 클릭하여 잠금 상태를 해제하실 수 있습니다.
    %p= link_to '계정 잠금 해제', unlock_url(@resource, :unlock_token => @resource.unlock_token)
